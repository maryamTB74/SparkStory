# Copy this file to .env and replace the placeholder values.
#
#   cp .env.sample .env
#
# .env is gitignored. Never commit real keys.
#
# Every variable below maps to a field in src/sparkstory/config.py.
# Variables not declared there are silently ignored (extra="ignore"), so if you
# add one here, add the matching field too.

# --- Required ------------------------------------------------------------
# Google AI Studio key, used for Gemini text generation (and, from the
# illustration session onward, image generation).
# Get one at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=AIzaSyExample000000000000000000000000000

# --- Optional: second provider -------------------------------------------
# xAI key, used by the grok-* entries. Only needed if a *_MODEL below names one.
# Worth having: when Gemini returns 503 UNAVAILABLE, no Gemini model helps, and
# a second provider is the only thing that does.
# Get one at: https://console.x.ai/
XAI_API_KEY=xai-Example00000000000000000000000000000

# --- Model selection ----------------------------------------------------
# One per agent, each a key of `llm_configs` in config.py. Separate variables so
# that giving the writer a stronger (more expensive) model than the planner is a
# config change rather than a code change.
#
# Available entries (keys of `llm_configs` in config.py):
#   gemini-3.5-flash   Gemini, newest. Returns 503 UNAVAILABLE under load.
#   gemini-2.5-pro     Gemini, slowest and most expensive.
#   grok-4             xAI. A different provider, so Gemini congestion does not
#                      affect it. Needs XAI_API_KEY.
#   grok-3-mini        xAI, smaller and cheaper.
PLANNER_MODEL=gemini-3.5-flash
PLOT_MODEL=gemini-3.5-flash
WRITER_MODEL=gemini-3.5-flash
RESEARCHER_MODEL=gemini-3.5-flash
OUTLINE_CRITIC_MODEL=gemini-3.5-flash-critic
PROSE_CRITIC_MODEL=gemini-3.5-flash-critic

# The all-Grok equivalent, if Gemini is returning 503:
#   PLANNER_MODEL=grok-3-mini
#   PLOT_MODEL=grok-3-mini
#   WRITER_MODEL=grok-3-mini
#   RESEARCHER_MODEL=grok-3-mini-researcher
#   OUTLINE_CRITIC_MODEL=grok-3-mini-critic
#   PROSE_CRITIC_MODEL=grok-3-mini-critic

# --- Revision loops ------------------------------------------------------
# Each loop runs N revisions and N+1 critiques, so every draft that can be
# returned has been judged. Consequence: 0 means "critique once, never revise",
# not "skip the critic" -- the safety gate still runs and can still fail a run.
MAX_OUTLINE_REVISIONS=2
MAX_PROSE_REVISIONS=2
MAX_REVIEWS_PER_PASS=5

# --- Database (Postgres + pgvector) --------------------------------------
# Required by retrieval and by per-child memory. docker-compose.yml brings up a
# matching pgvector instance; `make migrate` creates the schema and `make ingest`
# fills the corpus.
#
# Note the host differs by where you run from: `localhost` from your machine,
# `postgres` (the service name) from inside the compose network. The compose file
# sets the in-network value for the server container itself, so this one is for
# host-side commands like `make migrate`.
DATABASE_URL=postgresql+psycopg://sparkstory:sparkstory_dev_password@localhost:5432/sparkstory

# Read by docker-compose.yml, not by the app. Override all three together if you
# change any, since DATABASE_URL above must agree with them.
# POSTGRES_USER=sparkstory
# POSTGRES_PASSWORD=sparkstory_dev_password
# POSTGRES_DB=sparkstory
# POSTGRES_PORT=5432

# --- Research and retrieval ----------------------------------------------
# The embedding model runs locally: no key, no network at query time. Build the
# index once before a grounded run:
#
#   uv run python scripts/ingest_knowledge.py
#
# MAX_RESEARCH_STEPS=0 skips research entirely, which is how you compare a
# grounded run against an ungrounded one on the same premise.
EMBEDDING_MODEL=potion-base-8M
MAX_RESEARCH_STEPS=4
RETRIEVAL_TOP_K=5

# --- Optional: the web tool ----------------------------------------------
# OFF by default. At MAX_WEB_SEARCHES=0 no web client is built and neither key
# below is read, so you can ignore this whole section unless you want it.
#
# What it is for: a premise the corpus knows nothing about -- submarines, say --
# is currently planned ungrounded, because a fact we cannot point at is one we
# will not stand behind. This gives such a fact a source.
#
# How a web fact earns its place, and why there are two keys:
#   1. Perplexity searches and returns claims, each with a URL. That URL is
#      written by the *model*, so at this point it is an assertion, not a source.
#   2. Firecrawl fetches the page and the claim is checked against what it
#      actually says. A fabricated URL dies here.
# VERIFY_WEB_CLAIMS=false skips step 2. Do not set it outside a test: it is the
# only thing checking step 1's URL, and an unverified fact still reaches a child.
#
# TAVILY_API_KEY is a fallback for step 1: if Perplexity fails or has no key,
# Tavily searches instead. Its URLs come structurally from the API rather than
# being written by a model, but step 2 still runs -- the fetch is what makes a
# claim checkable, whichever provider found it.
#
# MAX_WEB_SEARCHES=3
# VERIFY_WEB_CLAIMS=true
# PERPLEXITY_API_KEY=pplx-Example0000000000000000000000000
# FIRECRAWL_API_KEY=fc-Example00000000000000000000000000
# TAVILY_API_KEY=tvly-Example000000000000000000000000000

# --- Illustration and narration ------------------------------------------
# Both tools are OFFERED BY DEFAULT. Set a flag false and that tool is not
# registered at all, so `list_tools` never shows it and a client cannot call it.
# That is the point: a deployment that must not spend money on images is unable
# to, rather than merely asked not to.
ILLUSTRATION_ENABLED=true
NARRATION_ENABLED=true

# The director writes the visual plan (a writing task) and the illustrator draws
# it (an image model), so they are named separately.
ILLUSTRATOR_MODEL=grok-image
ILLUSTRATION_DIRECTOR_MODEL=grok-3-mini

# Checks a drawn page against the character portrait it was conditioned on, so
# the same character stays the same across pages. Reuses the zero-temperature
# critic entry. JUDGE_PAGES=false keeps the portrait check (2 calls a book, and
# it gates the expensive stage) while skipping the per-page one.
CONSISTENCY_JUDGE_MODEL=grok-3-mini-critic
JUDGE_PAGES=true

# Reads a finished book aloud. Defaults to xAI rather than Google deliberately --
# see the mixed-provider warning below.
NARRATOR_MODEL=grok-speech

# --- Memory and evaluation ------------------------------------------------
# The extractor decides what a finished book established about a child, for the
# next book to use. The judge scores a finished book offline and never runs
# during a story.
MEMORY_EXTRACTOR_MODEL=gemini-3.5-flash
JUDGE_MODEL=gemini-3.5-flash-critic

# !! Mixed providers is the trap that has cost the most time here. Several
# stages default to Google while the all-Grok block above pins everything else to
# xAI. A stage left on a provider you have no key for fails -- and MEMORY_
# EXTRACTOR_MODEL fails *open*: it stores nothing, no error surfaces, and the run
# looks completely normal. Set every *_MODEL together, always.

# --- Server ---------------------------------------------------------------
# SERVER_HOST and SERVER_PORT apply to the HTTP transport only; stdio ignores
# both. 127.0.0.1 refuses connections from outside the machine, which is what you
# want locally -- in Docker it must be 0.0.0.0 or the container answers nothing.
# SERVER_NAME=SparkStory MCP Server
# SERVER_VERSION=          # read from package metadata when unset
# SERVER_HOST=127.0.0.1
# SERVER_PORT=8000

# --- Observability (Opik) -------------------------------------------------
#
# Off, and the app is unchanged while it is. Nothing imports opik unless
# OPIK_ENABLED is true, so a run that does not trace pays nothing for the
# dependency -- no litellm, no second openai client loaded into the process.
#
# What it buys: an 11-call book becomes a trace tree instead of a diff between
# two run directories. Every model call in both pipelines appears as a span,
# keyed by the same request_id that is in the logs, so a log line leads to its
# trace.
#
# A missing or blank key here is not an error. It logs a warning and the book is
# written untraced, because an observability problem must never cost a book.
#
# OPIK_ENABLED=true
# OPIK_API_KEY=
# OPIK_WORKSPACE=
# OPIK_PROJECT_NAME=sparkstory

# --- Logging -------------------------------------------------------------
# LOG_LEVEL controls SparkStory's own modules.
# LOG_LEVEL_DEPENDENCIES controls noisy third-party libraries (httpx, google,
# fastmcp, ...) separately, so request spam does not drown out our logs.
LOG_LEVEL=INFO
LOG_LEVEL_DEPENDENCIES=WARNING
